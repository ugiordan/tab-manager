{
  "name": "tab-manager",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "bridge:start": "npm run start -w packages/bridge",
    "bridge:dev": "npm run dev -w packages/bridge"
  }
}
